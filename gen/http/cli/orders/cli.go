// Code generated by goa, DO NOT EDIT.
//
// orders HTTP client CLI support package
//
// Command:
// $ goa gen github.com/Nihal1203/order-management-system/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	ordersc "github.com/Nihal1203/order-management-system/gen/http/orders/client"
	usersc "github.com/Nihal1203/order-management-system/gen/http/users/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"orders placeorder",
		"users (register|login)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "orders placeorder --body '{\n      \"itemid\": \"Nobis quam qui deleniti tempore repudiandae deleniti.\",\n      \"quantity\": 161822941,\n      \"size\": \"Velit minus suscipit dolorem quod.\"\n   }' --token \"Voluptatum necessitatibus facere praesentium.\"" + "\n" +
		os.Args[0] + " " + "users register --body '{\n      \"city\": \"Ipsum magni qui quia.\",\n      \"email\": \"keara@ryan.info\",\n      \"firstName\": \"In ut esse.\",\n      \"lastName\": \"Eligendi quae voluptatem rem odit optio maiores.\",\n      \"line1\": \"Et illo quasi minus quisquam excepturi.\",\n      \"line2\": \"Sint quasi.\",\n      \"password\": \"Voluptate atque non consequatur.\",\n      \"state\": \"Ut consequuntur quis.\",\n      \"zipcode\": 1566885917\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		ordersFlags = flag.NewFlagSet("orders", flag.ContinueOnError)

		ordersPlaceorderFlags     = flag.NewFlagSet("placeorder", flag.ExitOnError)
		ordersPlaceorderBodyFlag  = ordersPlaceorderFlags.String("body", "REQUIRED", "")
		ordersPlaceorderTokenFlag = ordersPlaceorderFlags.String("token", "REQUIRED", "")

		usersFlags = flag.NewFlagSet("users", flag.ContinueOnError)

		usersRegisterFlags    = flag.NewFlagSet("register", flag.ExitOnError)
		usersRegisterBodyFlag = usersRegisterFlags.String("body", "REQUIRED", "")

		usersLoginFlags    = flag.NewFlagSet("login", flag.ExitOnError)
		usersLoginBodyFlag = usersLoginFlags.String("body", "REQUIRED", "")
	)
	ordersFlags.Usage = ordersUsage
	ordersPlaceorderFlags.Usage = ordersPlaceorderUsage

	usersFlags.Usage = usersUsage
	usersRegisterFlags.Usage = usersRegisterUsage
	usersLoginFlags.Usage = usersLoginUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "orders":
			svcf = ordersFlags
		case "users":
			svcf = usersFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "orders":
			switch epn {
			case "placeorder":
				epf = ordersPlaceorderFlags

			}

		case "users":
			switch epn {
			case "register":
				epf = usersRegisterFlags

			case "login":
				epf = usersLoginFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "orders":
			c := ordersc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "placeorder":
				endpoint = c.Placeorder()
				data, err = ordersc.BuildPlaceorderPayload(*ordersPlaceorderBodyFlag, *ordersPlaceorderTokenFlag)
			}
		case "users":
			c := usersc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "register":
				endpoint = c.Register()
				data, err = usersc.BuildRegisterPayload(*usersRegisterBodyFlag)
			case "login":
				endpoint = c.Login()
				data, err = usersc.BuildLoginPayload(*usersLoginBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// ordersUsage displays the usage of the orders command and its subcommands.
func ordersUsage() {
	fmt.Fprintln(os.Stderr, `Service is the orders service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] orders COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    placeorder: Placeorder implements placeorder.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s orders COMMAND --help\n", os.Args[0])
}
func ordersPlaceorderUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] orders placeorder", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Placeorder implements placeorder.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "orders placeorder --body '{\n      \"itemid\": \"Nobis quam qui deleniti tempore repudiandae deleniti.\",\n      \"quantity\": 161822941,\n      \"size\": \"Velit minus suscipit dolorem quod.\"\n   }' --token \"Voluptatum necessitatibus facere praesentium.\"")
}

// usersUsage displays the usage of the users command and its subcommands.
func usersUsage() {
	fmt.Fprintln(os.Stderr, `This is a user service.This server contains all user related functionalities.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] users COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    register: Register implements register.`)
	fmt.Fprintln(os.Stderr, `    login: Login implements login.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s users COMMAND --help\n", os.Args[0])
}
func usersRegisterUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] users register", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Register implements register.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "users register --body '{\n      \"city\": \"Ipsum magni qui quia.\",\n      \"email\": \"keara@ryan.info\",\n      \"firstName\": \"In ut esse.\",\n      \"lastName\": \"Eligendi quae voluptatem rem odit optio maiores.\",\n      \"line1\": \"Et illo quasi minus quisquam excepturi.\",\n      \"line2\": \"Sint quasi.\",\n      \"password\": \"Voluptate atque non consequatur.\",\n      \"state\": \"Ut consequuntur quis.\",\n      \"zipcode\": 1566885917\n   }'")
}

func usersLoginUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] users login", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Login implements login.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "users login --body '{\n      \"email\": \"jacinto_jenkins@blickberge.info\",\n      \"password\": \"Id quo temporibus aut delectus voluptatem.\"\n   }'")
}
